import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#f4f4f9',
      light: '#b8dbd9',
      dark: '#121212',
    },
    secondary: {
      main: '#2f4550',
    },
  },
});

export default theme;
